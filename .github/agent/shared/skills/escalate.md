# escalate

Escalate an item to human maintainers when the agent cannot safely decide.

Allowed params:

```json
{
  "reason": "short explanation",
  "labels": ["needs-human"]
}
```

Default label: `needs-human`.

