# assign_issue

Assign the pull request to one or more GitHub users.

Allowed params:

```json
{
  "assignees": ["alice"]
}
```

Use when the PR author is unknown or the PR needs a clear owner for follow-up.
Prefer CODEOWNERS-driven assignment when possible.
