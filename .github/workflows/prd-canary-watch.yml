# ─────────────────────────────────────────────────────────────────────────────
# prd-canary-watch.yml — post-deploy 3σ canary observer (reusable workflow).
# ─────────────────────────────────────────────────────────────────────────────
# Runs every 15 minutes. When a recent prd deploy exists (consumer
# convention: a `PRD_LAST_DEPLOY` repo var written by prd.yml), pull
# current Sentry rate, compute baseline, run canary-watch atom.
#
# STATUS: scaffold — Sentry fetch is a stub. The math (canary-watch
# atom) is fully tested. Consumers fill in the Sentry queries by mirroring
# the pattern in actions/prd/check-error-rate (P2-15).
# ─────────────────────────────────────────────────────────────────────────────
name: prd-canary-watch

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:
  workflow_call:
    secrets:
      sentry-token:
        required: false

permissions: {}

concurrency:
  group: prd-canary-watch
  cancel-in-progress: false

jobs:
  watch:
    name: 3σ Canary Watch
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      issues: write
    steps:
      - uses: step-security/harden-runner@f808768d1510423e83855289c910610ca9b43176
        with: { egress-policy: audit }
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with: { persist-credentials: false }

      - name: Has recent deploy?
        id: recent
        env:
          LAST: ${{ vars.PRD_LAST_DEPLOY }}
        run: |
          set -euo pipefail
          if [ -z "$LAST" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "::notice title=Canary Skipped::no recent prd deploy marker"
            exit 0
          fi
          if last_epoch="$(date -u -d "$LAST" +%s 2>/dev/null)"; then :;
          elif last_epoch="$(date -u -j -f '%Y-%m-%dT%H:%M:%SZ' "$LAST" +%s 2>/dev/null)"; then :;
          else
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "::warning title=Canary Skipped::cannot parse $LAST"
            exit 0
          fi
          age_min=$(( ( $(date -u +%s) - last_epoch ) / 60 ))
          if [ "$age_min" -gt 30 ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "::notice title=Canary Skipped::deploy too old (age=${age_min}min)"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Fetch current + history rates (stub)
        if: steps.recent.outputs.skip != 'true'
        id: rates
        env:
          TOKEN:   ${{ secrets.sentry-token || secrets.SENTRY_TOKEN }}
          ORG:     ${{ vars.SENTRY_ORG }}
          PROJECT: ${{ vars.SENTRY_PROJECT }}
        run: |
          set -euo pipefail
          if [ -z "$TOKEN" ] || [ -z "$ORG" ] || [ -z "$PROJECT" ]; then
            echo "::warning title=Canary::Sentry credentials missing — emitting stub rates"
            echo "current=0" >> "$GITHUB_OUTPUT"
            echo "history="  >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "::warning title=Canary Stub::Sentry fetch not implemented — workflow is a scaffold"
          echo "current=0" >> "$GITHUB_OUTPUT"
          echo "history="  >> "$GITHUB_OUTPUT"

      - name: Run canary-watch atom
        if: steps.recent.outputs.skip != 'true'
        uses: ./actions/prd/canary-watch
        with:
          current-rate:     ${{ steps.rates.outputs.current }}
          history-rates:    ${{ steps.rates.outputs.history }}
          regression-fails: "false"
