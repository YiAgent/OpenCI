# release.yml — event entry for tagged releases.
# `git push origin v*` runs marketplace + docker release jobs by default.
name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      mode:
        required: false
        type: string
        default: both

permissions:
  contents: write
  packages: write
  id-token: write
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    uses: YiAgent/OpenCI/.github/workflows/reusable-release.yml@94dc602cc7f9a30d8805033a500124c2378f1538
    secrets: inherit
    with:
      mode: ${{ inputs.mode || 'marketplace' }}
      image-name: ${{ vars.IMAGE_NAME || github.event.repository.name }}
      registry: ghcr.io
      runner: blacksmith-2vcpu-ubuntu-2404
