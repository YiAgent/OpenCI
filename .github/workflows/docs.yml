# docs.yml — event entry for documentation builds + Pages publish.
name: docs

on:
  pull_request:
    paths:
      - "docs/**"
      - "**/*.md"
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "**/*.md"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: false

jobs:
  docs:
    uses: ./.github/workflows/reusable/docs.yml
    with:
      build-cmd: ${{ vars.DOCS_BUILD_CMD }}
      docs-dir: ${{ vars.DOCS_DIR || 'docs' }}
      site-dir: ${{ vars.DOCS_SITE_DIR || 'site' }}
      runner: blacksmith-32vcpu-ubuntu-2404
