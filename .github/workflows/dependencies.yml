# dependencies.yml — event entry that flips Renovate patch PRs to auto-merge.
name: dependencies

on:
  pull_request_target:
    types: [opened, labeled, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
concurrency:
  group: dependencies-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: false

jobs:
  deps:
    uses: YiAgent/OpenCI/.github/workflows/reusable-deps.yml@f9b5e83fc2dc47f1b5fe15f32d9c433b6a3ecf74
    with:
      runner: blacksmith-2vcpu-ubuntu-2404
